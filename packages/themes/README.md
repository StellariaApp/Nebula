# @stellaria/nebula-themes

Temas oficiales (`nebula-light`/`nebula-dark` + presets `sober`/`playful`), Zod schema del `NebulaTheme` (ADR-006) y utilidades de validación.
El contrato de tipos vive en `@stellaria/nebula-tokens`; aquí viven sus instancias y el schema runtime.
