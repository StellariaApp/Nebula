# @stellaria/nebula-web

Componentes web de Nebula: Vanilla Extract (zero-runtime) + hooks de React Aria (a11y) + motion v12.
Deps pesadas aisladas en subpaths (`/charts`, `/dnd`, `/editor`…) según ADR-014.
