import type { ReactElement } from "react";
import { Calendar, Clock } from "../../glyphs/index.js";

export const CALENDAR_ICON: ReactElement = <Calendar />;

export const CLOCK_ICON: ReactElement = <Clock />;
