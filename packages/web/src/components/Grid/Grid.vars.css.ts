import { createVar } from "@vanilla-extract/css";

export const columns = createVar();
export const gutter = createVar();
export const grow = createVar();
export const colSpan = createVar();
export const colOffset = createVar();
