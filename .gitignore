# deps
node_modules/

# builds
# lo reescribe Next segun si lo ultimo fue build o dev; no es de nadie
next-env.d.ts
dist/
build/
storybook-static/
.next/
out/
*.tsbuildinfo
test/

# turbo
.turbo/

# expo / native
.expo/
android/
ios/

# spike descartable (ADR-012 — se elimina tras el veredicto)
spike/

# env & logs
# lo escribe el CI con el token; nunca debe versionarse
.npmrc
.env
.env.*
!.env.example
*.log

# OS / editor
.DS_Store
Thumbs.db

# Figma MCP (descargas de assets)
.figma/

# salida de size-limit --json, que se pide a mano al revisar presupuestos
size-report.json

# indice de busqueda: lo genera Pagefind desde el HTML construido en cada build
**/public/pagefind/

# diffs del gate visual: los escribe jest-image-snapshot al fallar y se regeneran
# en cada pasada. El baseline de al lado (__snapshots__/visual/<plataforma>/) SI se versiona
**/__snapshots__/visual/__diff__/
