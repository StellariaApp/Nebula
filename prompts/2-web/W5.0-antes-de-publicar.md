# W5.0 — Lo que falta antes de publicar

> Paso previo a [W5.1](W5-prompts.md), que ya tiene escrita la mecánica de release. Este archivo es
> lo **otro**: lo que WN dejó sin verificar y lo que la ficha por componente destapó. Se hace antes
> porque puede obligar a cambiar componentes, y después de v1 eso deja de ser gratis.

## Lo que falta, medido sobre el repo (2026-08-11)

| Tramo                               | Qué falta                                                                 | Por qué antes de publicar                                |
| ----------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------- |
| **P0 · Los dos gates sin correr**   | `a11y` (axe sobre 96 stories) y `visual` (ADR-037) **nunca se corrieron** | Un fallo de a11y publicado es una incidencia de terceros |
| **P1 · El contrato sin documentar** | **69 componentes sin una línea de JSDoc**; 82 props de 1.852 con doc      | Es lo primero que ve un consumidor, y ahora se ve vacío  |
| **P2 · El sitio**                   | `nebula.stellaria.app` no sirve nada; el buscador no busca                | El sitio es la carta de presentación del paquete         |

Los números salen de `apps/web/generated/api.json` → `gaps` y de `catalog.json`, no de una
estimación: 69 en `noJsdoc`, 5 en `defaultUnknown`, 4 en `noDestructuring`, 87 de 158 con `.md`.

## Prompt de arranque — pegar en una sesión limpia

```text
Actúa como ingeniero de UI en C:\Users\Skr13\Documents\GitHub\Nebula.

Fase W5.0 — lo que falta antes de publicar. NO se añaden componentes, NO se cambia identidad
visual. Se verifica lo que WN no verificó y se documenta el contrato. La publicación en sí
es W5.1 y W5.2, que ya están escritos: NO los ejecutes aquí.

EL PRINCIPIO QUE ORDENA LA FASE:
  **Lo que hoy se corrige gratis, después de v1 cuesta una versión mayor.**
  Por eso el orden es: primero lo que se decide y repinta todo (los temas), luego lo que
  puede obligar a cambiar un componente (a11y, visual), luego lo que solo se escribe
  (JSDoc), y el sitio al final.

ANTES DE TOCAR NADA, LEE EN ESTE ORDEN
  1. CLAUDE.md — guardrails, convenciones (ADR-019) y política de trabajo con el propietario.
  2. prompts/2.3-web-normalize/REVISION-final-antes-de-W5.md, sección «Estado para quien lea
     esto sin abrir nada» — de dónde vienes y, sobre todo, qué NO se verificó.
  3. docs/03-a11y-motion-performance.md §4 — los nueve gates y por qué el visual no está en CI.
  4. docs/02-theming.md §2 (contrato NebulaTheme) y §temas oficiales — T0 empieza ahí.
  5. docs/05-roadmap.md §W5 — el gate de la fase siguiente, para saber a qué apuntas.

REGLA DE MEDIDA, que este proyecto ya se ha saltado tres veces:
  Ningún número entra en un commit sin haberlo medido en el repo. Los recuentos de español de
  WB fallaron por estimar; el de JSDoc resultó ser 7 veces la estimación. Mide y di el número.

--------------------------------------------------------------------------------
T0 · LOS TEMAS                       (LO PRIMERO — es decision, no ejecucion)
--------------------------------------------------------------------------------
Empieza por aqui porque el nombre de un tema es API publica: añadir uno despues de
v1 es menor, pero QUITARLO o RENOMBRARLO es breaking, y cambiar el eje de marca
repinta todo el catalogo.

LO QUE HAY HOY, medido en el repo y no supuesto:

  - `@stellaria/nebula-themes` exporta DOS: `light` y `dark` (officialThemeNames).
    Es lo unico que v1 publicaria tal cual esta.
  - `packages/demos/src/themes/products.ts` define OCHO productos x dark/light = 16
    temas: rosette, stellaria, polaris, lagrange, aurora, nova, eclipse y cosmos.
    Viven en el paquete de demos, NO en el publicable.
  - El panel del sitio muestra SIETE (nebula + 6): **eclipse y cosmos existen y no
    aparecen en ningun sitio**. O entran o se borran; hoy son codigo muerto vistoso.
  - `check:contrast` corre sobre los oficiales y el tema de humo. Los 16 de producto
    **nunca han pasado el gate AA**. Si alguno se publica, entra en el gate primero.
  - 20 paletas generadas 50-950 en tokens (indigo, violet, green, yellow, red, blue,
    orange, teal, pink, cyan, lime, grape, rose, gold, sand, slate, brown, light,
    dark, gray) desde PALETTE_SEEDS en tools/palette-gen.
  - ADR-020 fija el eje: indigo #3F37C9 -> violet #9D4EDD, dark-first.

LAS PREGUNTAS QUE HAY QUE CERRAR CON EL PROPIETARIO, con recomendacion cada una:

  1. Cuantos temas publica v1. Hoy 2. Si la respuesta es «los productos tambien»,
     se mueven de demos a packages/themes, entran en el gate de contraste y pasan a
     ser contrato.
  2. Falta alguno. Un catalogo se juzga por su tema por defecto: mira si el par
     light/dark cubre lo que un consumidor espera (alto contraste? una variante mas
     sobria para producto interno?) antes de que el numero se congele.
  3. Sobran. eclipse y cosmos son los candidatos evidentes: nadie los ve.
  4. Se toca el color. Cambiar una semilla es `pnpm gen:palette from "#hex" --name x`
     y regenera su paleta entera; cambiar las de ADR-020 cambia la identidad de todo.
     Es AHORA o es una version mayor.

COMO SE TRABAJA ESTE TRAMO:
  - No toques ningun tema sin respuesta. Presenta el estado medido, las opciones y tu
    recomendacion, y espera — es la politica de CLAUDE.md y aqui es literal.
  - Cualquier cambio de semilla o de lista de temas: `pnpm gen:palette regen`,
    `pnpm check:contrast` sobre TODOS los temas resultantes, y ADR que lo registre.
    palettes.ts es generado; no se edita a mano.
  - Si un tema de producto se publica, su ficha en docs/02 §temas oficiales se
    actualiza en el mismo PR.

Salida del tramo: la lista definitiva de temas de v1, escrita en un ADR, con el gate
de contraste verde sobre todos ellos.

--------------------------------------------------------------------------------
P0 · LOS DOS GATES QUE NUNCA CORRIERON        (despues de T0, y no es negociable)
--------------------------------------------------------------------------------
Estado: el barrido de ranuras de WN cerró con build/typecheck/lint/test, check:slots,
check:contrast y size en verde, y dejó escrito que `pnpm turbo a11y` y el gate visual de
ADR-037 no se ejecutaron NI UNA VEZ. Son 96 archivos de stories.

  pnpm --filter playground-web build     # el storybook estático que los dos consumen
  pnpm turbo a11y                        # axe sobre todas las stories
  pnpm turbo visual                      # regresión visual (baseline win32)

Qué esperar, y qué hacer con ello:
  - axe va a encontrar cosas. Cada hallazgo se arregla en el COMPONENTE, no en la story: una
    story adaptada para pasar el gate es un gate que ya no sirve.
  - El visual tiene baseline solo de win32 y por eso no corre en CI. Vale para ver diffs; NO
    regeneres baseline para que pase. Un diff por encima del 0,1 % se mira, se explica y se
    decide.
  - Si un arreglo de a11y cambia el DOM de un componente, ese es exactamente el trabajo que
    esta fase existe para hacer AHORA. Después de v1 es un breaking change.

Salida del tramo: los dos gates corridos, cada hallazgo arreglado o justificado por escrito, y
el estado real anotado en docs/NOTAS-PARA-REVISAR.md.

--------------------------------------------------------------------------------
P1 · EL CONTRATO SIN DOCUMENTAR               (en paralelo con P0; es volumen)
--------------------------------------------------------------------------------
Medido en apps/web/generated/api.json → gaps:
  - 69 componentes sin UNA sola línea de JSDoc en su contrato
  - 82 props documentadas de 1.852
  - 5 props con el valor por defecto indeterminable
  - 4 componentes sin destructuring, así que el generador no puede leer sus defectos
  - 87 de 158 con `<Nombre>.md` interno

Ya no es una estadística: desde ADR-127 existe la ficha por componente y la columna de
descripción sale VACÍA en 158 páginas del sitio. Arreglarlo es el trabajo con más superficie
visible de toda la fase.

Las reglas están decididas y no se reabren:
  - ADR-105 — el JSDoc de API pública NO es un comentario: es la documentación que el
    generador publica. Se escribe con ese peso.
  - ADR-114 — se escribe en INGLÉS. El resto de docs/ sigue en español.
  - ADR-019 — nada de comentarios sueltos en el código; lo que pida explicación larga va al
    `<Nombre>.md` de al lado.
  - Orden por lo que se lee: props propias de los componentes más usados, después las ranuras
    —que ya son las mejor documentadas del catálogo—, y lo raro al final.

No inventes semántica. Si una prop no se entiende leyendo el componente, míralo en su story
del playground antes de describirla: una descripción equivocada es peor que ninguna.

Salida del tramo: `pnpm gen:docs` regenerado y commiteado junto al cambio (lo exige el gate de
frescura, docs/03 §4), y `noJsdoc` bajando en cada commit, medido y dicho.

--------------------------------------------------------------------------------
P2 · EL SITIO
--------------------------------------------------------------------------------
apps/web ya declara metadata, sitemap (172 URLs), robots, JSON-LD y tarjetas sociales
apuntando a https://nebula.stellaria.app (ADR-131), y **el dominio ya está montado** (2026-08-11).

  - El origen sale de NEXT_PUBLIC_SITE_URL; una preview o un staging lo ponen por entorno sin
    tocar código.
  - `/preview/*` está fuera del sitemap y del robots a propósito: son las muestras que la ficha
    enmarca en un iframe, no páginas con contenido.
  - El buscador sigue inerte y ADR-109 está en BORRADOR con un bloqueo propio: desde ADR-122
    las rutas son dinámicas, así que `next build` no deja HTML que Pagefind pueda indexar. O se
    resuelve —indexar desde el MDX, o prerenderizar una instantánea solo para el índice— o el
    campo se quita del cromado antes de publicar. Hoy es una promesa que no cumple, y eso en la
    portada de una librería se nota.

--------------------------------------------------------------------------------
LO QUE NO ENTRA AQUÍ
--------------------------------------------------------------------------------
  - La publicación: changesets, auditoría de exports, paquete paraguas, READMEs de consumo y
    la verificación en un Next 16 virgen son W5.1 y W5.2, ya escritos.
  - Componentes nuevos, identidad visual y refactors de forma: eso era WN y está cerrada.
  - Las muestras de las fichas: 140 con muestra viva, 9 en iframe y 9 con prosa. No hay hueco.
  - Native. N1 requiere W5, no al revés.
```

## Decisiones del propietario que tocan esta fase

Están en `docs/NOTAS-PARA-REVISAR.md`. Ninguna impide empezar, pero **las dos primeras hay que
cerrarlas antes de v1**:

- **D-2** — los cinco componentes cuyo `radius` apunta a un elemento interior (`Image`,
  `ImageGallery`, `Skeleton`, `EditorImage`, `Progress`). Está escrito en ADR-119 y falta el visto
  bueno. Si se migran es cambio de API, y después de publicar deja de ser barato.
- **El gate visual en CI** — pide el «entorno único» que ADR-037 §3 nombra sin elegir: un contenedor
  con fuentes fijadas que **sustituya** al baseline de win32, no que conviva con él.
- **`pnpm format:check` fuera de CI** — es un gate razonable; hoy fallaría por archivos en curso.

## Deuda anotada, que no bloquea

- **La caché de dev de Turbopack se corrompe**: `action-icon`, `tooltip`, `popover` y `menu` dieron
  500 en `next dev` con el mismo `dist` que producción sirve sin un fallo. Se cura con
  `rm -rf apps/web/.next/dev/cache` y reiniciar.
- **`site-background.tsx` contradice la tabla de ADR-129**: el código va con `density="md"` y
  `translucency` 2/4; el ADR fija `sm`/1 para el cromado. Uno de los dos tiene que ceder.
- **Los enlaces del carril no llevan icono**, así que en la barra inferior de móvil y en el carril
  mini de tablet se ven vacíos. Su contenedor además reporta `scrollWidth === clientWidth` en móvil,
  de modo que tampoco se desplaza hasta el activo.
- **El `iframe` de las superficies no repinta** al cambiar el tema desde el panel: hereda por
  `localStorage` al cargar y ahí se queda.

## Estado para quien lea esto sin abrir nada

**WN cerrada del todo.** El sitio está entero: seis secciones (ADR-127), cromado fijo con su cristal,
ficha por componente para los 158 —140 con muestra viva, 9 en marco y 9 con prosa— y metadata con
tarjetas sociales (ADR-131).

**Gates en verde al escribir esto**: `turbo build typecheck lint` 32/32, `web test` 1.278 tests en 141
archivos, `check:slots`, `check:contrast` 158/158 y `size` 10/10.

**Los dos gates que faltan son `a11y` y `visual`**, y son justamente los que pueden obligar a cambiar
un componente.

**Y antes que ellos, los temas**: `@stellaria/nebula-themes` publica hoy `light` y `dark`, mientras
`packages/demos` mantiene ocho productos —dos de ellos, `eclipse` y `cosmos`, que no se ven en ningún
sitio— y ninguno de los dieciséis ha pasado el gate de contraste. Cuántos temas lleva v1 es una
decisión del propietario, no un detalle de implementación: el nombre de un tema es contrato.
