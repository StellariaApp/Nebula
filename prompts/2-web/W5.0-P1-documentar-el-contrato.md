# W5.0 · P1 — Documentar el contrato del catálogo

> **Sesión exclusiva.** Este tramo se trabaja en paralelo a otra sesión que está en P2 (el sitio).
> Lee «Qué NO tocar» antes de escribir nada: hay archivos compartidos y un generado que se pisa.

## Estado medido (2026-08-11, tras la tanda 1)

| Medida                                                     | Valor              |
| ---------------------------------------------------------- | ------------------ |
| Props del catálogo **documentadas**                        | **833 de 3.136 — 27 %** |
| Componentes en `gaps.noJsdoc` (cero props propias con doc) | **43** de 158      |
| Componentes con **menos de un tercio** documentado         | **114**            |
| `defaultUnknown`                                           | 5                  |
| `noDestructuring`                                          | 4                  |
| Componentes con `<Nombre>.md`                              | 87 de 158          |

Los números salen de `apps/web/generated/api.json` → `gaps` y de recorrer `own` + `slots` de cada
componente, incluidos sus `subComponents`. **Mídelos otra vez antes de empezar**: la tanda 1 ya
cambió el suelo.

```bash
node -e "
const api=require('./apps/web/generated/api.json');
let doc=0,tot=0;
function W(l){for(const c of l||[]){const p=[...(c.own||[]),...(c.slots||[])];
  doc+=p.filter(x=>x.doc).length; tot+=p.length; W(c.subComponents);}}
W(api.components);
console.log(doc+' de '+tot+' ('+Math.round(doc/tot*100)+' %) · noJsdoc '+api.gaps.noJsdoc.length);
"
```

## Prompt de arranque — pegar en una sesión limpia

```text
Actúa como ingeniero de UI en C:\Users\Skr13\Documents\GitHub\Nebula.

Fase W5.0, tramo P1: documentar el contrato público del catálogo web. NO se añaden componentes,
NO se cambia comportamiento, NO se toca identidad visual. Solo se escribe el JSDoc que el
generador publica, y se corrigen los huecos que impiden generarlo.

Otra sesión está trabajando P2 (el sitio) EN PARALELO. Respeta «Qué NO tocar».

ANTES DE ESCRIBIR NADA, LEE EN ESTE ORDEN
  1. CLAUDE.md — convenciones (ADR-019) y política de trabajo con el propietario.
  2. docs/adr/ADR-105 — el JSDoc de API pública NO es un comentario: es la página que el
     generador publica. Se escribe con ese peso, y prohíbe explícitamente el JSDoc que
     parafrasea el nombre de la prop.
  3. docs/adr/ADR-114 — se escribe en INGLÉS. El resto de docs/ sigue en español.
  4. packages/web/src/components/Section/Section.types.ts — es el estándar de la casa y el
     mejor documentado del catálogo (13 de 19). Imítalo.
  5. tools/docs-gen/src/api.ts §~340 — cómo se decide qué cuenta como documentado.

QUÉ ES UNA BUENA LÍNEA, Y QUÉ NO
  Se escribe LO QUE EL TIPO NO PUEDE DECIR. El tipo ya dice el nombre, la forma y si es
  opcional; repetirlo es ruido que además parece documentación.

    MAL   /** The size of the component. */          size?: Size
    MAL   /** Whether the field is disabled. */      disabled?: boolean
    BIEN  /** Clamps the text to this many lines with an ellipsis. Beats `truncate`, and only
           *  applies above zero. Needs a width to work against: inside a flex child, that
           *  usually means `miw={0}`. */

  Las cuatro cosas que merecen tinta, por orden de valor:
    · a qué otra prop gana o pierde  (`lines` gana a `truncate`; `fluid` ignora `size`)
    · cuándo NO aplica               (`preventGrowOverflow` no hace nada sin `grow`)
    · qué pasa si se omite           (sin `title` la región cae a `aria-label`)
    · el guardrail                   (`glass` en todas las secciones da un fondo uniforme,
                                      que es lo contrario del efecto)
  Y `@default` cuando el valor sale de la desestructuración o de una constante.

NO INVENTES SEMÁNTICA. Si una prop no se entiende leyendo el componente, ábrela en su story
del playground o en su `.md` antes de describirla. Una descripción equivocada es peor que
ninguna: la primera se publica como verdad, la segunda solo deja un hueco.

ORDEN DE TRABAJO — por lo que un consumidor toca de verdad
  Tanda 2 · Utilidades de render      Portal, VisuallyHidden, Conditional, Valid, Omit,
                                      FocusTrap, Transition, Collapse
  Tanda 3 · Tipografía y marcas       Anchor, Code, Kbd, Mark, Highlight, GradientText,
                                      ThemeIcon, List, Blockquote
  Tanda 4 · Acciones y campos         los que el gate ya aprueba pero están casi vacíos:
                                      Button 5/15, ActionIcon 1/9, Avatar 1/10, Card 1/12,
                                      ButtonCopy 1/14, Badge 3/13, Burger 1/9
  Tanda 5 · Efectos y superficies     GlassSurface, GradientBorder, NoiseOverlay, StarField,
                                      Overlay, Skeleton, Loader, BlurOverlay
  Tanda 6 · Dominio y el resto        CurrencyDisplay 18, StarField 16, HoverCard 14,
                                      ModalDelete 13, Scroll 12, Segment 12, DateDisplay 11,
                                      Reveal 11, y lo que quede

  La tanda 1 (primitivos de composición) ya está hecha en el commit 0bccf2f. No la rehagas;
  úsala de referencia de tono.

LOS OTROS DOS HUECOS, que son bugs y no falta de prosa
  · defaultUnknown (5): PieChart, RadarChart, TrendIndicator, CreatableSelect,
    VirtualizedSelect. El generador saca el defecto de la desestructuración y falla donde el
    valor se calcula. Se arregla con `@default` en el JSDoc, que ADR-105 ya permite.
  · noDestructuring (4): AreaChart, BarChart, LineChart, SearchableSelect. No desestructuran
    sus props, así que el generador no puede leer NINGÚN defecto. Mira si desestructurar es
    seguro; si no lo es, `@default` a mano.

CÓMO SE MIDE Y SE CIERRA CADA TANDA
  set -o pipefail
  pnpm turbo build typecheck lint test --filter=@stellaria/nebula-web
  pnpm gen:docs            # OBLIGATORIO: el gate de frescura (docs/03 §4.9) falla si no
  # y mide, y DILO en el commit:
  node -e "..."            # el recuento de arriba

  Un commit por tanda, con `docs(web):` de scope. En el mensaje van LAS DOS cifras: props
  documentadas (la honesta) y `noJsdoc` (la del gate).

POR QUÉ DOS CIFRAS, Y NO UNA
  `noJsdoc` cuenta COMPONENTES con cero props documentadas, así que una sola línea saca a un
  componente del contador aunque le queden nueve props vacías. Peor: el JSDoc de
  `BoxOwnProps.className` lo HEREDA todo lo que extiende Box, de modo que la tanda 1 sacó del
  contador a `Code` (1 de 4) y `GradientText` (1 de 6) sin tocarlos. Si solo miras `noJsdoc`,
  el trabajo parece terminado con el 27 % escrito.

QUÉ NO TOCAR — hay otra sesión trabajando
  · apps/web/**            TODO el sitio es de la otra sesión.
  · tools/docs-gen/**      salvo que el propietario apruebe el gap `thinlyDocumented`.
  · packages/tokens/**, packages/themes/**, tools/contrast-check/**
  · Cualquier .tsx o .css.ts de componente: este tramo escribe en `.types.ts` y en `<Nombre>.md`.
    Si para documentar una prop necesitas cambiar el componente, PARA y pregunta.

  EXCEPCIÓN, y es la que puede chocar: `apps/web/generated/api.json` lo regenera `pnpm
  gen:docs` y lo comprometen las dos sesiones. Si al hacer `git pull` sale conflicto ahí, NO
  lo resuelvas a mano: acepta cualquier versión, corre `pnpm gen:docs` y commitea el
  resultado. Es un generado; la fuente de verdad es el código.

REGLAS QUE YA COSTARON UN ERROR
  · `git add` SIEMPRE con rutas explícitas. Nunca `git add -A` ni una carpeta entera.
  · Antes de commitear, `git diff --cached --name-only` y míralo.
  · Ningún número entra en un commit sin haberlo medido. Este proyecto ya se lo saltó tres
    veces, y la estimación de JSDoc resultó ser 7 veces menor que la real.
```

## Criterio de salida

Lo decide el propietario, y hoy está sin decidir:

- **El original** (prompt de la fase): `noJsdoc` bajando en cada commit. Ya se cumple, y por lo
  dicho arriba no significa gran cosa.
- **El propuesto**: props documentadas por encima de un umbral —60 % sería 1.880 de 3.136— y
  ningún componente por debajo de un tercio. Requiere añadir `gaps.thinlyDocumented` a
  `tools/docs-gen`, que está propuesto y sin ejecutar.

Mientras no se decida, **mide y reporta las dos**.

## Lo que este tramo NO incluye

- Los `.md` internos. ADR-105 los exige **solo** cuando hay una decisión no deducible del código, y
  fijó que 73 de 158 no lo necesitan. No persigas el 158: busca al revés, componentes con una
  decisión rara en el código y sin `.md` que la explique. El de `Progress` es el estándar.
- Traducir nada. El JSDoc va en inglés y el resto de `docs/` sigue en español.
- Los gates de P0, que ya están corridos y en verde.
