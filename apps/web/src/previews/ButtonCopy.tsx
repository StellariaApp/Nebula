import { ButtonCopy } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <ButtonCopy value="pnpm add @stellaria/nebula-web" /> };

export default preview;
