import { FieldError } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <FieldError>That address is taken</FieldError> };

export default preview;
