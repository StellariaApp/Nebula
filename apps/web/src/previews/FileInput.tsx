import { FileInput } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <FileInput w={280} label="Attachment" placeholder="Pick a file" />,
};

export default preview;
