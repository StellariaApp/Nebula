import { Burger } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <Burger opened={false} openLabel="Open menu" closeLabel="Close menu" />,
};

export default preview;
