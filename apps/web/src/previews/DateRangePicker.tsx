import { DateRangePicker } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <DateRangePicker w={300} label="Period" /> };

export default preview;
