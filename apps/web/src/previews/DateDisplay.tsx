import { DateDisplay } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <DateDisplay value="2026-08-11T09:30:00.000Z" preset="long" /> };

export default preview;
