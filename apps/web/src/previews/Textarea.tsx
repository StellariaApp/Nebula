import { Textarea } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <Textarea w={280} label="Notes" placeholder="What changed?" /> };

export default preview;
