import { Banderole } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <Banderole w={320}>Nebula is in pre-release.</Banderole> };

export default preview;
