import { TagsInput } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <TagsInput w={300} label="Tags" defaultValue={["web", "native"]} />,
};

export default preview;
