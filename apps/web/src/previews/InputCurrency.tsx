import { InputCurrency } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <InputCurrency w={240} label="Amount" currency="EUR" /> };

export default preview;
