import { Stat } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <Stat label="Web components" value="158" description="in v1" /> };

export default preview;
