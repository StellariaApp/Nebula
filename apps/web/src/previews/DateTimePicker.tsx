import { DateTimePicker } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <DateTimePicker w={300} label="Starts at" /> };

export default preview;
