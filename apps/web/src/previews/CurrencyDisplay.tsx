import { CurrencyDisplay } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <CurrencyDisplay amount={1299.5} currency="EUR" /> };

export default preview;
