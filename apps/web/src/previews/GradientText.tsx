import { GradientText } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <GradientText fz="h3">Zero forks.</GradientText> };

export default preview;
