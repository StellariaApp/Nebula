import { Avatar, Indicator } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: (
    <Indicator label="3" size="sm">
      <Avatar>NB</Avatar>
    </Indicator>
  ),
};

export default preview;
