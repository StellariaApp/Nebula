import { Dropzone } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <Dropzone w={320} label="Drop the theme JSON here" /> };

export default preview;
