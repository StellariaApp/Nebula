import { UnstyledButton } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <UnstyledButton>Bare button</UnstyledButton> };

export default preview;
