import { PasswordInput } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <PasswordInput w={280} label="Password" /> };

export default preview;
