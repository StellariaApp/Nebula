import { NumberInput } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <NumberInput w={200} label="Seats" defaultValue={3} /> };

export default preview;
