import { Blockquote } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <Blockquote w={360}>The contract lives in the tokens.</Blockquote>,
};

export default preview;
