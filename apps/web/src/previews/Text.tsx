import { Text } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <Text>The quick brown fox jumps over the lazy dog.</Text> };

export default preview;
