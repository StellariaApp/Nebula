import { Image } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <Image src="/logo.svg" alt="Nebula" w={160} h={80} fit="contain" />,
};

export default preview;
