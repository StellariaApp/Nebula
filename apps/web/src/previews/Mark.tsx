import { Mark, Text } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: (
    <Text>
      The theme reaches <Mark>the whole visual surface</Mark>.
    </Text>
  ),
};

export default preview;
