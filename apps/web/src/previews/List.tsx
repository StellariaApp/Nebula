import { List } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: (
    <List>
      <List.Item>Web and React Native</List.Item>
      <List.Item>One contract per component</List.Item>
    </List>
  ),
};

export default preview;
