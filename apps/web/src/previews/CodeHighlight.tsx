import { CodeHighlight } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = {
  base: <CodeHighlight code={`pnpm add @stellaria/nebula-web`} lang="bash" w={360} r="lg" />,
};

export default preview;
