import { QuickAction } from "@stellaria/nebula-web";
import { STAR } from "./kit";
import type { Preview } from "./types";

const preview: Preview = { base: <QuickAction label="Reconcile" icon={STAR} /> };

export default preview;
