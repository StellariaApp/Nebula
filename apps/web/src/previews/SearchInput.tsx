import { SearchInput } from "@stellaria/nebula-web";
import type { Preview } from "./types";

const preview: Preview = { base: <SearchInput w={280} placeholder="Search components" /> };

export default preview;
